"""Perception for VLM-guided manipulation: camera geometry and vision backends.

Nothing here is imported by the core. The Claude backend needs the
``anthropic`` package (extra ``vlm``); the oracle needs MuJoCo (extra ``sim``).
"""
