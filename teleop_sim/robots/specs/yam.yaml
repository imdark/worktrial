# YAM with its stock gripper: a composition, not a description of its own.
#
# Swapping the end effector is this file's `end_effector` line. Placing the
# robot in a cell is the run config's `robot.scene` (or `scene:` here); the
# real robot uses exactly this composition with no scene.

name: yam
arm: ../arms/yam.yaml
end_effector: ../end_effectors/yam_linear.yaml
